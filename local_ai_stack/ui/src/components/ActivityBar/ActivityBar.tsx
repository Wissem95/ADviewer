// Stub — sera complété en Task 7 (FileTree, LLMStatus, GitPanel, SprintBoard).
export function ActivityBar() {
  return (
    <aside
      className="w-60 border-r border-border bg-panel overflow-y-auto"
      aria-label="Activity bar"
    >
      <div className="p-3 text-muted text-xs">Activity Bar (Task 7)</div>
    </aside>
  );
}
