# Checkpoint — Plan 5B terminé (2026-05-08)

> Plan source : `docs/superpowers/plans/2026-04-20-plan-5B-verify-streaming.md`
> Pré-requis : Plan 5A complet (mode SIMPLE fonctionnel).

## Résultat

Pipeline mode SIMPLE est maintenant **robuste en production** :
- VERIFY étendu : pytest + vitest + cargo + ruff/eslint en parallèle.
- Retry automatique Stage5 ↔ Stage7 (max 3 tentatives) avec feedback erreurs au LLM.
- Rollback git stash automatique après 3 retries rouges.
- Streaming token-par-token côté backend + UI (bulle progressive).
- Bouton Stop fonctionnel (clic + Cmd+./Ctrl+.) → cancellation backend → rollback.
- Budget cap par pipeline ($1.00 par défaut) avec UI jauge tricolore.

## Compteurs tests

| Suite      | Verts | Cible Plan 5B | Delta vs Plan 5A |
|------------|-------|---------------|------------------|
| pytest     | 305   | 230+          | +41 (264 → 305)  |
| vitest     | 134   | 125+          | +13 (121 → 134)  |
| cargo      | n/a   | OK            | non lancé local  |

## Tasks livrées (8/8)

| Task | Sujet                                                            | Commit    |
|------|------------------------------------------------------------------|-----------|
| 1    | `tools/run_tests` wrappers pytest/vitest/cargo/lint              | `251a11e` |
| 2    | `Stage7Verify` complet (lint + pytest + vitest + cargo parallèle)| `1070a93` |
| 3    | Retry loop Stage5 ↔ Stage7 + retry_context + rollback            | `daf69df` |
| 4    | Backend streaming LLM (litellm stream=True + fallback)           | `826a240` |
| 5    | UI streaming `chat_token` + `StreamingBubble`                    | `0e58d4a` |
| 6    | Stop button + cancellation (`asyncio.CancelledError`)            | `ffbc4dc` |
| 7    | Budget cap par pipeline + UI `BudgetIndicator`                   | `c2d1d92` |
| 8    | Tests E2E retry/cancel/budget + tag `v2.1.0-alpha.2`             | (en cours)|

## Fonctionnalités livrées

### Backend
- `backend/tools/run_tests.py` : 4 wrappers async avec timeout, parsing passed/failed, stdout tronqué 3000 chars.
- `Stage7Verify` étendu : dispatch automatique (.py→ruff, .ts/.tsx→eslint, .rs→cargo) + parallélisme via `asyncio.gather`.
- `VerifyResult` enrichi : `lint_errors`, `test_errors`, `runners_summary`.
- `Pipeline._retry_until_green_or_max` : boucle Stage5→Stage7 jusqu'à 3 tentatives, injection erreurs VERIFY dans le prompt Stage5 via `ctx.retry_context`.
- `Stage5Execute._retry_hint` : bloc "RETRY (tentative #N/3) — VERIFY a échoué…" injecté au LLM.
- `backend/streaming.py` : `stream_llm_response(llm_id, messages, on_token)` qui itère sur `litellm.acompletion(stream=True)`.
- `LLMManager.call_with_fallback_streaming` : streaming + fallback chain + rate-limit.
- `Pipeline` gère `asyncio.CancelledError` avec rollback shieldé.
- `backend/budget_tracker.py` : `BudgetTracker` avec `track`, `track_cost`, `cap_exceeded`, `would_exceed`.
- Pipeline `BudgetExceeded` → rollback automatique si cap dépassé.
- Handler WS `pipeline_stop` dans `main.py` qui `.cancel()` la task active.

### UI
- `pipelineStore` étendu : `streamingBuffer/Stage/LLM`, action `onChatToken`, action `stop(reason)`.
- `StreamingBubble` : bulle progressive avec curseur clignotant.
- `BudgetIndicator` : jauge tricolore ($ accumulé / cap avec %), role=progressbar.
- `TraceViewer` intègre `BudgetIndicator` + `StreamingBubble` + raccourci Cmd+. / Ctrl+. avec listener keydown global.
- Listener WS sur event `chat_token` connecté dans `connectPipelineStore`.

### Tests
- `test_tools_run_tests.py` : 9 tests (pytest/vitest/lint/cargo + timeout + binaire absent).
- `test_stage_7_verify_full.py` : 9 tests (lint, pytest, dispatch eslint/.ts, cargo si .rs, etc.).
- `test_stage_5_retry.py` : 3 tests (1 retry vert, 3 retries rouges + rollback, retry_context injecté).
- `test_streaming.py` : 6 tests (on_token concaténation, chunks vides, fallback chain).
- `test_pipeline_cancellation.py` : 3 tests (cancel avant/après/pendant Stage5).
- `test_budget_tracker.py` : 7 tests (accumulation, cap_exceeded, would_exceed, llm inconnu).
- `test_pipeline_e2e_verify_retry.py` : 2 tests E2E (cancellation mid-pipeline, happy path post-5B).
- UI : `StreamingBubble.test.tsx` (5), `BudgetIndicator.test.tsx` (6), `TraceViewer.test.tsx` +2 tests Stop.

## Limitations restantes (à clore en 5C-5F)

- **Mode MEDIUM et COMPLEX** : toujours vides dans `stages_by_mode`. Plans 5C/5D rempliront.
- **AgentLoop legacy** : `Orchestrator.handle()` n'utilise toujours pas le Pipeline. Plan 5D Step 11.3.
- **Settings UI** : clés API et budget cap configurables uniquement via code. Plan 5E.
- **Pas de calibration coût réel** : `STAGE_TOKEN_ESTIMATES` reste statique. Plan 5E.
- **Pas de bundle distributable** : Plan 5F.
- **Mode Ollama** : pas branché. Plan 5F.

## État git

Branche `main`, +69 commits sur `origin/main` (à pousser sur `localcoder-ide` via subtree split). Tag attendu : `v2.1.0-alpha.2`.
